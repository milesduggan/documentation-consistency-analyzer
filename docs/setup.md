# Setup

## Environment

This project uses Gemini only.

Set your API key locally:

GEMINI_API_KEY=your-key-here

The key is entered at runtime in the browser and is never committed.
No OpenAI keys are used.
